#!/usr/bin/env python
# -*- coding: utf-8 -*-

def mapping(*args):
    mapping_keys={}
    print "Mapping keys\nType exit to quit"
    while True:
        #here the mapping stuff
        something=raw_input("> ").upper()
        #condition to break the loop
        if something=="EXIT":break
    pass
    #return a dict
    return mapping_keys
